@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# 필요한 의존성들을 찾습니다 (필요시 추가)
# find_dependency(Boost REQUIRED COMPONENTS system thread)

include("${CMAKE_CURRENT_LIST_DIR}/BT_LibraryTargets.cmake")

check_required_components(BT_Library)
